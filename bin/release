#!/usr/bin/env bash
# bin/release <build-dir>

BUILD_DIR=$1

export ES_USE_IPV4="true"

cat << EOF
---
default_process_types:
  web: /usr/share/elasticsearch/bin/elasticsearch -Des.path.conf=$HOME/app -Des.http.port=$PORT -Des.path.data=$ES_DATA_DIR -Djava.net.preferIPv4Stack=true
EOF
